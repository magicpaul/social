$('small.error').hide();
$(".new-status-box").removeClass('open').val('');
$('.input-box').removeClass('error');
$('#statuses').prepend("<%= escape_javascript(render 'create', :status => @status) %>");
$(".status-outer.new").fadeIn().removeClass('new');